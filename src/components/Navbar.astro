---
import UserIcon from "./icons/User.astro";
import BriefcaseIcon from "./icons/Briefcase.astro";
import CodeIcon from "./icons/Code.astro";
import MailIcon from "./icons/Mail.astro";
import GithubIcon from "./icons/Github.astro";
import LinkedinIcon from "./icons/Linkedin.astro";

import Route from "./navbar/Route.astro";
import type { RouteDTO } from "../types/RouteDTO";

const routes: RouteDTO[] = [
  { path: "/about", icon: UserIcon },
  { path: "/experience", icon: BriefcaseIcon },
  { path: "/jobs", icon: CodeIcon },
  { path: "/contact", icon: MailIcon },
];
const contactRoutes: RouteDTO[] = [
  { path: "https://github.com/lcasass3", icon: GithubIcon },
  {
    path: "https://www.linkedin.com/in/leon-casas-187504266/",
    icon: LinkedinIcon,
  },
];
---

<nav
  class="bg-background border-r border-r-primary/80 w-1/12 h-full left-0 relative py-20 flex flex-col justify-between"
>
  <ul class="grid w-full justify-center gap-y-16">
    {routes.map((route) => <Route route={route} />)}
  </ul>

  <ul class="grid w-full justify-center gap-y-16 bottom-0">
    {contactRoutes.map((route) => <Route route={route} />)}
  </ul>
</nav>
